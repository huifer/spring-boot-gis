server:
  port: 9001


spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/test?useUnicode=yes&characterEncoding=UTF-8&useSSL=false&serverTimezone=UTC
    password: root
    username: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jackson:
    serialization:
      INDENT_OUTPUT: true
  redis:
    host: localhost
    port: 6379
    database: 2


mybatis:
  type-aliases-package: com.huifer.springbootgis.entity
  mapper-locations: classpath:mapperxml/*.xml

logging:
  level:
    com.example.demo.mapper: debug


useing:
  mysql: false
